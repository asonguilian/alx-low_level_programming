/**
  * string_toupper - converts all lowercas letters in string to uppercase
  *@s: input string
  * Return: output string
  */

char *string_toupper(char *s)
{
	int i;

	i = 0;
	while (s[i] != '\0')
	{
		if (s[i] >= 97 && s[i] <= 122)
			s[i] = s[i] - 32;
		i++;
	}
	return (s);
}
