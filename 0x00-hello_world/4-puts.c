#include<stdio.h>

/**
 * main-entry point
 *prints a message on std output and exits
 *Return: always 0 (success)
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
