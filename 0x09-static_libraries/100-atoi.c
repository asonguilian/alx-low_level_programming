/**
  * _atoi - converts a string to an integer
  * @s: string to be converted
  * Return: 0 if there are no numbers in hte string
  * otherwise, integer valueof number present in the string
  */

int _atoi(char *s)
{
	int value = 0;

	if (*s == '\0')
		return (0);
	return (value);
}
