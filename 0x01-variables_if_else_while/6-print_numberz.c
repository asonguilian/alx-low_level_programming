#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i, n;

	i = 48;
	n = 10;
	while (i <= 57)
	{
		putchar(i);
		i++;
	}
	putchar(n);
	return (0);
}
